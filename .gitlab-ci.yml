pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  - mkdir -p public/.well-known/acme-challenge/
  - cp key.test public/.well-known/acme-challenge/
  artifacts:
    paths:
    - public
  only:
  - master